#include <stdio.h>
#include <stdlib.h>
#include "graph.h"
#include "file.h"


int main() {
    readFile();
    /*
    Graph* graph = createGraph(4);
    addEdge(graph, 0, 1);
    addEdge(graph, 0, 2);
    addEdge(graph, 0, 3);
    addEdge(graph, 1, 2);
    addEdge(graph, 1, 0);
    addEdge(graph, 2, 0);
    addEdge(graph, 3, 0);

    printGraph(graph);

    destroyGraph(graph);
    printGraph(graph);*/
    return 0;
}